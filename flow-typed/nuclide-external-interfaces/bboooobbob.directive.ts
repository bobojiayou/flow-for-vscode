import { Directive, ElementRef } from '@angular/core';

@Directive({
    selector: 'bboooobbob',
})

export class function toUpperCase() { [native code] }boooobbobDirective implements Validator {
    constructor(private el: ElementRef) { }
}

